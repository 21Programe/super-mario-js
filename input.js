export class Input{constructor(){this.keys=new Set();this.just=new Set();this.pad=null;this.touch={left:false,right:false,run:false,jump:false,jumpHeld:false};const map={ArrowLeft:'left',KeyA:'left',ArrowRight:'right',KeyD:'right',ShiftLeft:'run',ShiftRight:'run',Space:'jump',ArrowUp:'jump',KeyW:'jump',ArrowDown:'down',KeyS:'down'};addEventListener('keydown',e=>{const k=map[e.code];if(k){if(!this.keys.has(k))this.just.add(k);this.keys.add(k);e.preventDefault()}});addEventListener('keyup',e=>{const k=map[e.code];if(k)this.keys.delete(k)});addEventListener('blur',()=>this.keys.clear());for(const [id,k] of [['btn-left','left'],['btn-right','right'],['btn-run','run'],['btn-jump','jump']]){const b=document.getElementById(id);if(!b)continue;const on=e=>{e.preventDefault();this.touch[k]=true;this.just.add(k)};const off=e=>{e.preventDefault();this.touch[k]=false};b.addEventListener('pointerdown',on);b.addEventListener('pointerup',off);b.addEventListener('pointercancel',off);b.addEventListener('pointerleave',off)}}
update(){if(navigator.getGamepads){const p=[...navigator.getGamepads()].find(Boolean);this.pad=p||null}}down(k){const p=this.pad;let v=this.keys.has(k)||this.touch[k];if(p){if(k==='left')v||=p.axes[0]<-.25||p.buttons[14]?.pressed;if(k==='right')v||=p.axes[0]>.25||p.buttons[15]?.pressed;if(k==='jump')v||=p.buttons[0]?.pressed||p.buttons[12]?.pressed;if(k==='run')v||=p.buttons[2]?.pressed}return !!v}pressed(k){const v=this.just.has(k);this.just.delete(k);return v}consume(k){this.just.delete(k)}}
